# Copy this file to `.env` and paste your token in. `.env` is gitignored —
# it never leaves your machine and never reaches GitHub.
#
#   cp .env.example .env
#   npm run instagram
#
# Get the token from docs/INSTAGRAM_SETUP.md, step 3.

INSTAGRAM_ACCESS_TOKEN=paste_your_long_lived_token_here
